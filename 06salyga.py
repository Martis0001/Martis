# val min
# koki laika matysime po 1 min

val = int(input('val'))
min = int(input('min'))

if val (23) and min(59+1)
    print (00:00)


val = int(input('Kiek valandu'))
min = int(input('Kiek minuciu'))
poMIN = min + 1
valPoMin = val + (poMin//60) 
poMin = poMin % 60
print(f'po minute bus{valPoMin} : {poMin}')

viskasMinPoMin = val * 60 + min + 1
poMin = viskasMinPoMin % 60
valPomin = (viskasMinPoMin // 60) % 24
prient(f'po )